# custom-elements-manifest-demo

This demonstrates generating a Custom Elements Manifest (CEM) file,
named `custom-elements.json`.

To get started, enter `npm install`.

To generate the CEM, enter `npm run cem`.

To run the app, enter `npm run dev` and browse `localhost:5173`.
